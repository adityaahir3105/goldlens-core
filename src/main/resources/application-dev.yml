# Development profile - verbose logging and local defaults
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.goldlens: DEBUG
    org.hibernate.SQL: DEBUG
